# Проект

ядро для интернет-магазина

## Описание:

E-commerce  — электронная торговля, или электронная коммерция. 
На данном этапе работы мы не будем реализовывать систему платежей, однако подготовим всё для того, 
чтобы у нас появилось ядро для интернет-магазина. 
В дальнейшем для этого ядра возможно будет реализовать любой интерфейс — от сайта до телеграм-бота.

### В проекте реализованны следующие классы и функции:

Создан базовый абстрактный класс с именем 
BaseProduct, который является родительским для классов продуктов.

Создан базовый абстрактный класс с именем 
BaseCategory. В него вынесены общие свойства для классов «Заказ» и «Категория».

Созданы классы Product, Category и Order
Добавлен класс CategoryIterator
Созданы два класса наследников класса Product: 
«Смартфон» (Smartphone) и «Трава газонная» (LawnGrass).
Создан класс-миксин MixinPrint, который при создании объекта, 
печатает в консоль информацию о том, от какого класса и с какими параметрами был создан объект.
  
Для класса Product определенны следующие свойства:
название (name),
описание (description),
цена (price),
количество в наличии (quantity).

  Для класса Category определенны следующие свойства:
название (name),
описание (description),
список товаров категории (products).

  Для класса Order определенны следующие свойства:
product: объект класса Product, представляющий приобретённый товар 
param quantity: количество приобретённого товара

  Класс «Смартфон» (Smartphone) расширен атрибутами:
производительность (efficiency), 
модель (model), 
объем встроенной памяти (memory), 
цвет (color).

  Класс «Трава газонная» (LawnGrass) расширен атрибутами: 
страна-производитель (country), 
срок прорастания (germination_period), 
цвет (color).

Для всех классов написаны тесты